<!-- SINH TỰ ĐỘNG bởi scripts/render_topic_prompts.py — không sửa tay -->
<!-- topics=716afa9b261d72ee invariants=b57c1e3ee6326156 renderer=prompt-library.v1 -->

# T5 — SHOP_PROFILE

- kind: `domain`
- anchor: `Shop`
- kế thừa: —

## Semantic refs

```text
measure.shop_rating | measure | rating_point | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_followers | measure | followers | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_items | measure | items | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_response_rate | measure | percent | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_response_time | measure | time | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_rating_good | measure | ratings | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_rating_normal | measure | ratings | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_rating_bad | measure | ratings | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.shop_cancellation_rate | measure | percent | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
dim.shop_official | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
dim.shop_vacation | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
```

## Relation paths

| path | relation | grain | fanout | dedupe |
| --- | --- | --- | --- | --- |
| `listing_to_shop` | `belongs_to` | listing_snapshot → listing_snapshot | none | — |

## Invariants

- `INV-DEDUPE-BEFORE-AGGREGATE` (hard) — `invariant.dedupe_before_aggregate`

<!-- render_budget_tokens=492 -->
