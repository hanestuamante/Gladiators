<!-- SINH TỰ ĐỘNG bởi scripts/render_topic_prompts.py — không sửa tay -->
<!-- topics=f84fa00979e93301 invariants=b57c1e3ee6326156 renderer=prompt-library.v1 -->

# T6 — CATALOG_STRUCTURE

- kind: `domain`
- anchor: `ProductListing`, `PlatformCategory`, `ShopCategory`
- kế thừa: —

## Semantic refs

```text
dim.platform_category_name | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=INV-FILTER-LITERAL-IS-DATASET-VALUE
dim.shop_category_name | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
dim.shop_category_parent | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
dim.shop_category_child | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
dim.platform_category_has_children | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
measure.shop_category_total | measure | listings | grain=listing_snapshot | agg=median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
```

## Relation paths

| path | relation | grain | fanout | dedupe |
| --- | --- | --- | --- | --- |
| `listing_platform_category` | `in_platform_category` | listing_snapshot → listing_snapshot_category | none | — |
| `listing_shop_shelf` | `in_shop_category` | listing_snapshot → listing_snapshot_x_shelf | duplicates_left_rows | one_row_per_listing |

## Invariants

- `INV-SHELF-NOT-PLATFORM-CATEGORY` (hard) — `invariant.shelf_not_platform_category`
- `INV-DEDUPE-BEFORE-AGGREGATE` (hard) — `invariant.dedupe_before_aggregate`

<!-- render_budget_tokens=264 -->
