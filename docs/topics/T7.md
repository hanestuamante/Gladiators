<!-- SINH TỰ ĐỘNG bởi scripts/render_topic_prompts.py — không sửa tay -->
<!-- topics=5611c9b568ebc866 invariants=b57c1e3ee6326156 renderer=prompt-library.v1 -->

# T7 — CONTENT_VARIATION

- kind: `domain`
- anchor: `ProductListing`, `Content`
- kế thừa: —

## Semantic refs

```text
measure.images_count | measure | images | grain=listing_snapshot | agg=sum,mean,median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
measure.variation_options_count | measure | options | grain=listing_snapshot | agg=sum,mean,median,min,max | filters=eq,lt,lte,gt,gte | binding=exposed_as_measure | invariant=-
dim.display_variation | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
dim.shopee_verified | dimension | dimension | grain=listing_snapshot | agg=- | filters=eq,in | binding=exposed_as_dimension | invariant=-
```

## Relation paths

| path | relation | grain | fanout | dedupe |
| --- | --- | --- | --- | --- |
| `listing_content` | `has_content` | listing_snapshot → listing_snapshot | none | — |
| `listing_variation` | `has_display_variation` | listing_snapshot → listing_snapshot | none | — |

## Invariants

- `INV-SNAPSHOT-SCOPE` (hard) — `invariant.snapshot_scope`

<!-- render_budget_tokens=177 -->
